/** 
 * Wrapper for ProductCardList
 */

import styled from 'styled-components';

const Wrapper = styled.div`
  width: 125%; 
  display: flex; 
  flex-direction: row;
  flex-wrap: wrap;
  margin:0;
  height: auto;
`;

export default Wrapper;